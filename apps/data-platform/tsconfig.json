{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Browser code, so the DOM comes along with the language lib.
    "lib": ["es2024", "dom", "dom.iterable"],
    "jsx": "react-jsx",
    // import.meta.env and the ?url / ?raw import forms.
    "types": ["vite/client", "node"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "vite.config.ts"]
}
